
name: 🚀 Deploy to App Store Connect

on:
  workflow_dispatch:

  push:
    tags:
      - 'release-*'

concurrency:
  group: appstore-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Rename → Compile Check → Deploy
    runs-on: macos-latest

    env:
      EXPORT_DIR:     ./build
      EXPORT_OPTIONS: ./ExportOptions.plist

      # ★★★ 换项目只改下面三行 ★★★
      TEAM_ID:        "VFQ3D59FGD"
      PROJECT_NAME:   "Uangkucatatan"
      BUNDLE_ID:      "com.uangkucatatan.app"

    steps:
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      # 1. Checkout
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      - name: 📥 Checkout
        uses: actions/checkout@v4

      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      # 2. Select latest Xcode
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      - name: 🔧 Select Xcode (iOS 26 SDK)
        run: |
          LATEST=$(ls -d /Applications/Xcode*.app 2>/dev/null | sort -V | tail -1)
          echo "Picked: $LATEST"
          sudo xcode-select -switch "$LATEST"
          echo "Xcode $(xcodebuild -version | head -1)"
          echo "iOS SDK $(xcrun --sdk iphoneos --show-sdk-version)"

      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      # 3. Setup ASC credentials (必须在编译前)
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      - name: 🔑 Setup App Store Connect
        env:
          ASC_API_KEY:   ${{ secrets.APP_STORE_CONNECT_API_KEY }}
          ASC_KEY_ID:    ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          mkdir -p ~/private_keys
          echo "$ASC_API_KEY" > ~/private_keys/AuthKey_${ASC_KEY_ID}.p8
          chmod 600 ~/private_keys/AuthKey_${ASC_KEY_ID}.p8

          cat > $EXPORT_OPTIONS << PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>app-store-connect</string>
              <key>teamID</key>
              <string>${TEAM_ID}</string>
              <key>uploadSymbols</key>
              <true/>
              <key>uploadBitcode</key>
              <false/>
              <key>manageAppVersionAndBuildNumber</key>
              <false/>
          </dict>
          </plist>
          PLIST

          echo "✅ ASC credentials ready"

      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      # 4. 重命名项目 → 编译验证 → 失败则终止
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      - name: 🔄 Rename Project & Compile Check
        env:
          ASC_KEY_ID:    ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          echo "═════════════════════════════════════"
          echo "  🔄 重命名项目"
          echo "═════════════════════════════════════"

          OLD_NAME=$(find . -maxdepth 1 -name '*.xcodeproj' -type d | head -1 | sed 's|^\./||; s|\.xcodeproj$||')
          echo "原项目名: $OLD_NAME"
          echo "新项目名: $PROJECT_NAME"
          echo "新 Bundle: $BUNDLE_ID"

          if [ "$OLD_NAME" = "$PROJECT_NAME" ]; then
            echo "项目名未变，跳过重命名"
          else
            if [ -d "$OLD_NAME" ]; then
              mv "$OLD_NAME" "$PROJECT_NAME"
              echo "✅ 文件夹: $OLD_NAME → $PROJECT_NAME"
            fi
            mv "${OLD_NAME}.xcodeproj" "${PROJECT_NAME}.xcodeproj"
            echo "✅ xcodeproj: ${OLD_NAME}.xcodeproj → ${PROJECT_NAME}.xcodeproj"
            PBXPROJ="${PROJECT_NAME}.xcodeproj/project.pbxproj"
            sed -i '' "s/${OLD_NAME}/${PROJECT_NAME}/g" "$PBXPROJ"
            echo "✅ pbxproj 内引用全部替换"
          fi

          PBXPROJ="${PROJECT_NAME}.xcodeproj/project.pbxproj"

          OLD_BUNDLE=$(grep -m1 'PRODUCT_BUNDLE_IDENTIFIER = ' "$PBXPROJ" | sed 's/.*PRODUCT_BUNDLE_IDENTIFIER = \(.*\);/\1/')
          if [ "$OLD_BUNDLE" = "$BUNDLE_ID" ]; then
            echo "Bundle ID 未变，跳过"
          else
            sed -i '' "s/PRODUCT_BUNDLE_IDENTIFIER = [^;]*;/PRODUCT_BUNDLE_IDENTIFIER = $BUNDLE_ID;/g" "$PBXPROJ"
            echo "✅ Bundle ID: $OLD_BUNDLE → $BUNDLE_ID"
          fi

          BUILD_NUMBER=${{ github.run_number }}
          sed -i '' "s/CURRENT_PROJECT_VERSION = [0-9]*;/CURRENT_PROJECT_VERSION = $BUILD_NUMBER;/g" "$PBXPROJ"

          # 替换 Team ID，确保与 ASC API Key 所属 Team 一致
          sed -i '' "s/DEVELOPMENT_TEAM = [A-Z0-9]*;/DEVELOPMENT_TEAM = ${TEAM_ID};/g" "$PBXPROJ"
          echo "✅ Team ID → ${TEAM_ID}"

          # --- 从 Xcode 项目读取版本号 ---
          VERSION=$(grep -m1 'MARKETING_VERSION = ' "$PBXPROJ" | sed 's/.*MARKETING_VERSION = \(.*\);/\1/')
          echo "Version: $VERSION（来自 Xcode 项目）"

          # 写入环境变量供后续步骤使用
          echo "VERSION=$VERSION" >> $GITHUB_ENV

          echo ""
          echo "修改后配置:"
          grep -E 'PRODUCT_BUNDLE_IDENTIFIER|PRODUCT_NAME|CURRENT_PROJECT_VERSION|MARKETING_VERSION' "$PBXPROJ" | sort -u | head -10

          echo ""
          echo "═════════════════════════════════════"
          echo "  🔨 编译验证"
          echo "═════════════════════════════════════"

          xcodebuild build \
            -scheme "$PROJECT_NAME" \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            -quiet

          echo ""
          echo "✅ 编译通过！继续打包上传..."

      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      # 5. Archive
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      - name: 📦 Archive
        env:
          ASC_KEY_ID:    ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          ARCHIVE_PATH="./build/${PROJECT_NAME}.xcarchive"

          # Archive 阶段不签名，签名由 Export 阶段完成（Distribution 证书无需设备）
          xcodebuild archive \
            -scheme "$PROJECT_NAME" \
            -archivePath "$ARCHIVE_PATH" \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO

          echo "✅ Archive done"

      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      # 5.5 删除 SPM 框架的供应商原始签名，强制 Export 重新签署
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      - name: 🧹 Strip Vendor Signatures
        run: |
          FRAMEWORKS_DIR="./build/${PROJECT_NAME}.xcarchive/Products/Applications/${PROJECT_NAME}.app/Frameworks"
          if [ -d "$FRAMEWORKS_DIR" ]; then
            # 1. 删除传统 _CodeSignature 目录
            find "$FRAMEWORKS_DIR" -name "_CodeSignature" -type d -exec rm -rf {} + 2>/dev/null || true
            # 2. 删除 xcframework .signature 签名包（Xcode 16+ 新格式）
            find "$FRAMEWORKS_DIR" -name "*.signature" -type d -exec rm -rf {} + 2>/dev/null || true
            find "$FRAMEWORKS_DIR" -name "*.signature" -type f -exec rm -f {} + 2>/dev/null || true
            # 3. 逐个 framework 兜底再清一遍
            find "$FRAMEWORKS_DIR" -name "*.framework" -type d | while read fw; do
              rm -rf "$fw/_CodeSignature" 2>/dev/null || true
              rm -rf "$fw"/*.signature 2>/dev/null || true
            done
            echo "✅ Stripped all vendor signatures (_CodeSignature + .signature)"
          else
            echo "ℹ️  No frameworks directory found, skipping"
          fi

      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      # 6. Export IPA
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      - name: 📤 Export IPA
        env:
          ASC_KEY_ID:    ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          xcodebuild -exportArchive \
            -archivePath "./build/${PROJECT_NAME}.xcarchive" \
            -exportPath "$EXPORT_DIR" \
            -exportOptionsPlist "$EXPORT_OPTIONS" \
            -allowProvisioningUpdates \
            -authenticationKeyPath ~/private_keys/AuthKey_${ASC_KEY_ID}.p8 \
            -authenticationKeyID "$ASC_KEY_ID" \
            -authenticationKeyIssuerID "$ASC_ISSUER_ID"

          echo "✅ IPA exported → $(ls -lh $EXPORT_DIR/*.ipa)"

      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      # 6.5 删除 IPA 中 Xcode 26 生成的 xcframework .signature 签名包
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      - name: 🧹 Strip Signatures from IPA
        run: |
          IPA=$(ls $EXPORT_DIR/*.ipa | head -1)
          echo "Stripping Signatures from: $IPA"

          TMP=$(mktemp -d)
          unzip -q "$IPA" -d "$TMP"

          # 检查并删除 Xcode 26 生成的 Signatures/ 目录
          if [ -d "$TMP/Signatures" ]; then
            echo "Found Signatures/ directory, removing..."
            ls -la "$TMP/Signatures/"
            rm -rf "$TMP/Signatures"
            echo "✅ Removed Signatures/"
          else
            echo "ℹ️  No Signatures/ directory found"
          fi

          # 重新打包 IPA
          rm -f "$IPA"
          ORIG_DIR=$(pwd)
          cd "$TMP" && zip -qry "$ORIG_DIR/$IPA" . && cd "$ORIG_DIR"
          rm -rf "$TMP"
          echo "✅ IPA repackaged → $(ls -lh $IPA | awk '{print $5}')"

      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      # 7. Save IPA as Artifact
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      - name: 💾 Upload IPA to Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.PROJECT_NAME }}-build${{ github.run_number }}
          path: ${{ env.EXPORT_DIR }}/*.ipa
          retention-days: 90

      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      # 8. Upload to App Store Connect
      # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      - name: ☁️ Upload to App Store Connect
        env:
          ASC_KEY_ID:    ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          IPA_FILE=$(ls $EXPORT_DIR/*.ipa | head -1)

          xcrun altool --upload-app \
            --type ios \
            --file "$IPA_FILE" \
            --apiKey "$ASC_KEY_ID" \
            --apiIssuer "$ASC_ISSUER_ID" \
            --verbose

          echo ""
          echo "══════════════════════════════════════════════"
          echo "  上传完成！去 App Store Connect 手动提审。"
          echo "  项目名: $PROJECT_NAME"
          echo "  Version: $VERSION (Build #${{ github.run_number }})"
          echo "  Bundle: $BUNDLE_ID"
          echo "══════════════════════════════════════════════"
