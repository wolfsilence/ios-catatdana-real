import Foundation

// OneClickResp
struct c: Codable {
    var ab: String? // token -> ab
    var efnsb: Bool? // sendVcode -> efnsb
    var xvupmmlv: String? // redirectUrl -> xvupmmlv // 重定向地址
    var mqfzkc: Int? // loginGuide -> mqfzkc // 1 已发送验证码(sendVcode为true) 2 需要填写密码登录 3 需要设置密码并注册
    var aipwiijgo: Bool? // isPlatformNew -> aipwiijgo // 是否是平台新客，前端发送埋点需要
    var hnnir: Bool? // hnnir ->
    var zcofb: Int? // zcofb ->
    var ke: Int64? // ke ->
    var fohnosow: Double? // fohnosow ->
    var lyejmw: Bool? // lyejmw ->
    var xkslau: Int? // xkslau ->
    var nwuv: Int64? // nwuv ->
    var gns: Int? // gns ->
    var vuyp: Double? // vuyp ->
}
