import Foundation

// RecgFaceReq
struct tgq: Codable {
    var prmp: String? // livenessId -> prmp
    var rruguses: String? // livenessImg -> rruguses
    var govizh: String? // imageID -> govizh
    var xplgj: String? // xplgj ->
    var dhfgxvsih: Int? // dhfgxvsih ->
    var ezzajsnv: String? // ezzajsnv ->
    var cn: Int? // cn ->
    var rfv: Int64? // rfv ->
    var hwnmxc: Int? // hwnmxc ->
    var hviur: Int? // hviur ->
    var nhbzy: Int64? // nhbzy ->
    var gyt: Double? // gyt ->
}
