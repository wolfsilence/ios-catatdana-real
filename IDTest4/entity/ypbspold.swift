import Foundation

struct ypbspold: Codable {
    // 电池电量百分⽐(0-100)
    var olkhcofke: String? // battery_pct -> olkhcofke // 0-100
    
    // 是否正在充电（1：是，0:否）
    var fkvrwg: Int? // is_charging -> fkvrwg
    
    // 是否usb充电（1：是，0:否）
    var jwebay: Int? // is_usb_charge -> jwebay
    
    // 是否充电器充电（1：是，0:否）
    var kewbxfxyj: Int? // is_ac_charge -> kewbxfxyj
    var lhu: Int64? // lhu ->
    var iudquvk: String? // iudquvk ->
    var evapetq: Double? // evapetq ->
    var laftpwx: Int? // laftpwx ->
    var xtwlo: Int64? // xtwlo ->
    var ghfomdns: Int64? // ghfomdns ->
    var lh: Int64? // lh ->
    var txdlmvzzm: Int? // txdlmvzzm ->
    var yvkohap: String? // yvkohap ->
}
